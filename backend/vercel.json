{
  "version": 2,
  "name": "native-server",
  "builds": [{ "src": "server.js", "use": "@vercel/node" }],
  "routes": [
    {
      "src": "/(.*)",
      "methods": ["GET", "POST", "PUT", "DELETE"],
      "dest": "/server.js",
      "headers": {
        "Access-Control-Allow-Origin": "*"
      }
    }
  ]
}
